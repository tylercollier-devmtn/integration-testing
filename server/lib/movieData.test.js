const movieData = require('./movieData');

describe('unit tests', () => {
  describe('create', () => {
    it('automatically passes in the current createdAt timestamp', () => {
      
    });
  });
});
